我将按照您的要求，对平台总后台的权限进行收缩，并增加详情查看功能。以下是具体的实施计划：

### 1. 订单管理权限收缩与详情查看
**目标文件**：`src/admin/views/OrderMgr.vue`
*   **权限收缩**：移除“核销”和“退款/取消”按钮及相关逻辑代码，确保主后台仅具备数据查看权限，无法干预订单状态。
*   **新增查看功能**：
    *   在列表操作列增加“查看”按钮（文字链接样式）。
    *   增加侧边抽屉（Drawer）组件，用于展示订单完整详情。
    *   **详情内容**：除了列表已有的订单号、场馆、金额外，补充展示用户信息（模拟）、支付方式（模拟）、交易流水号（模拟）等详细字段，以只读形式展示。

### 2. 列表页“查看”功能覆盖
**目标文件**：`src/admin/views/VenueMgr.vue`（场馆管理）、`src/admin/views/CouponMgr.vue`（优惠券管理）
*   **场馆管理**：
    *   在操作列增加“查看”按钮。
    *   复用现有的编辑抽屉，增加 `isView` 状态。当点击“查看”时，将表单设置为禁用（Disabled）状态，仅供浏览。
*   **优惠券管理**：
    *   在操作列增加“查看”按钮。
    *   同样复用编辑抽屉，在查看模式下禁用表单输入。

### 实施步骤
1.  **修改 OrderMgr.vue**：清理操作按钮，实现详情抽屉。
2.  **修改 VenueMgr.vue**：添加查看模式逻辑。
3.  **修改 CouponMgr.vue**：添加查看模式逻辑。
4.  **验证**：确认主后台无法操作订单，且各列表页均能通过抽屉查看完整详情。